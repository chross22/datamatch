# Open a CEFI OPeNDAP endpoint, with a readable failure

Open a CEFI OPeNDAP endpoint, with a readable failure

## Usage

``` r
cefi_open(url)
```

## Arguments

- url:

  the `dodsC` URL

## Value

an open `ncdf4` handle; the caller closes it
