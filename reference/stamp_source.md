# Stamp a fetched object with its source

Stamp a fetched object with its source

## Usage

``` r
stamp_source(x, source, detail)
```

## Arguments

- x:

  the object to stamp

- source:

  the source family, e.g. `"hycom"`

- detail:

  the archive, dataset or version within it

## Value

`x`, stamped
