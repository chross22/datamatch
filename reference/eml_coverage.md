# Geographic and temporal coverage of a matched table

Geographic and temporal coverage of a matched table

## Usage

``` r
eml_coverage(x, flat)
```

## Arguments

- x:

  the object, for its bounding box

- flat:

  the same without geometry, for its time columns

## Value

a list in EML `coverage` shape
