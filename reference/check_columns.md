# Check that requested columns exist

Check that requested columns exist

## Usage

``` r
check_columns(env_dat, vars)
```

## Arguments

- env_dat:

  the source object

- vars:

  requested columns

## Value

`NULL`, invisibly; called for the error
