# The THREDDS server CEFI is published on

Held in one place because it appears in three: the catalog walk that
finds filenames, the OPeNDAP endpoint that reads them, and the error
messages that say where a failed request went.

## Usage

``` r
cefi_server()
```

## Value

the server root, with no trailing slash
