# The period token a frequency is published under

The period token a frequency is published under

## Usage

``` r
obdaac_period(frequency)
```

## Arguments

- frequency:

  `"daily"` or `"monthly"`

## Value

the token in an OB.DAAC filename
