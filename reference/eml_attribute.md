# Describe one column as an EML attribute

Describe one column as an EML attribute

## Usage

``` r
eml_attribute(name, values)
```

## Arguments

- name:

  the column name

- values:

  the column itself, which decides the measurement scale

## Value

a list in EML `attribute` shape
