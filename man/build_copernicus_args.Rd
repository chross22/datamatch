% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copernicus_cli.R
\name{build_copernicus_args}
\alias{build_copernicus_args}
\title{Build the argument vector for a \verb{copernicusmarine subset} call}
\usage{
build_copernicus_args(
  dataset_id,
  vars,
  bb,
  depth,
  time,
  ofile,
  overwrite = TRUE,
  dry_run = FALSE,
  log_level = "ERROR"
)
}
\arguments{
\item{dataset_id}{\if{html}{\out{<char>}} Copernicus dataset identifier}

\item{vars}{\if{html}{\out{<char>}} variable codes to request}

\item{bb}{bounding box: a named list or vector with \code{xmin}, \code{xmax}, \code{ymin},
\code{ymax}, or an \code{sf}/\code{sfc} object to take the bounding box of}

\item{depth}{\if{html}{\out{<numeric>}} length-2 depth range in metres}

\item{time}{\if{html}{\out{<Date>}} or \if{html}{\out{<char>}} length 1 or 2; a single value requests one instant}

\item{ofile}{\if{html}{\out{<char>}} full path of the file to write}

\item{overwrite}{\if{html}{\out{<logical>}} pass \code{--overwrite}}

\item{dry_run}{\if{html}{\out{<logical>}} pass \code{--dry-run}, which validates the request against
the API without downloading anything}

\item{log_level}{\if{html}{\out{<char>}} one of the client's levels: DEBUG, INFO, WARN, ERROR,
CRITICAL, QUIET. \code{ERROR} is the default rather than \code{QUIET}, because the
client says \emph{why} a request failed at ERROR and says nothing at all at
QUIET. A date past the end of a reanalysis, for instance, exits non-zero
with no output under QUIET, leaving the caller an empty failure to explain.}
}
\value{
character vector of arguments, suitable for \code{\link[=system2]{system2()}}
}
\description{
Kept separate from the call itself so the arguments can be inspected and
tested without contacting the API.
}
\details{
Long-form flags are used throughout (\code{--dataset-id} rather than \code{-i}). The
short forms are aliases the client has changed before, and a renamed flag
fails as an unrecognised option rather than as anything self-explanatory.
}
\keyword{internal}
