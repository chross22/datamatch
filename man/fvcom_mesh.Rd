% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fvcom.R
\name{fvcom_mesh}
\alias{fvcom_mesh}
\title{Read an FVCOM mesh on its own, with no data on it}
\usage{
fvcom_mesh(
  archive = "GOM3",
  bounding_box = NULL,
  what = c("polygons", "nodes", "elements"),
  date = NULL
)
}
\arguments{
\item{archive}{which archive's mesh: a name from \code{\link[=fvcom_archives]{fvcom_archives()}}, or a spec
from \code{\link[=fvcom_archive]{fvcom_archive()}}}

\item{bounding_box}{\if{html}{\out{<list>}} optional named list with \code{xmin}, \code{xmax}, \code{ymin},
\code{ymax}, or an \code{sf}/\code{sfc} object. \code{NULL} returns the whole mesh, which for
GOM3 is 90,415 triangles.}

\item{what}{\if{html}{\out{<char>}} \code{"polygons"}, \code{"nodes"}, or \code{"elements"}}

\item{date}{the month whose mesh to read, for an archive that remeshes.
Ignored for a single-mesh archive.}
}
\value{
an \code{sf} object: \code{POLYGON} for \code{"polygons"}, \code{POINT} otherwise, in
EPSG:4326
}
\description{
\code{\link[=accessFVCOM]{accessFVCOM()}} returns values at points — nodes for scalars, element
centroids for velocities. That is what \code{\link[=matchData]{matchData()}} needs, but it is not the
mesh: it is a scatter of the mesh's points, and drawing it shows dots where
the grid is triangles. This returns the triangles themselves, so the grid can
actually be plotted.
}
\section{Why the points are not enough}{

An FVCOM mesh is a triangulation, and which nodes form each triangle lives in
a connectivity array (\code{nv}) that a point fetch never reads. Without it there
is no way to draw a cell boundary, shade a cell by its value, or show how the
resolution changes across a shelf — all of which are the usual reasons for
looking at an unstructured model in the first place.

The result carries no time dimension and no covariates. It is the grid.
}

\section{What comes back}{

\itemize{
\item \code{"polygons"} (the default) — one \code{POLYGON} per element, with the
element index and \code{DEPTH}, the mean of its three nodes' bathymetry. This
is what to plot.
\item \code{"nodes"} — the mesh nodes as \code{POINT}s, with \code{DEPTH}. Scalars such as
\code{SST} and \code{BOTS} live here.
\item \code{"elements"} — the element centroids as \code{POINT}s. Velocities and
stresses live here.
}
}

\section{Bounding box}{

A triangle is kept when \strong{any} of its three vertices falls inside the box, so
the returned mesh covers the box rather than stopping short of it. The edge is
therefore ragged, and a triangle may extend a little beyond what was asked
for. Clipping to the box exactly would cut triangles into shapes the model
does not have.
}

\section{Meshes that move}{

\code{GOM3} has one mesh for its whole record. \code{GOM7} does not — it is operational
output and remeshes between files, carrying 198,594 nodes in January 2025 and
207,081 from March. For an archive like that the mesh belongs to a particular
month, so \code{date} says which; it defaults to the start of the record.
}

\examples{
\dontrun{
bb <- list(xmin = -70, xmax = -66, ymin = 41, ymax = 44)

mesh <- fvcom_mesh(bounding_box = bb)
plot(sf::st_geometry(mesh))                 # the grid itself
plot(mesh["DEPTH"], border = NA)            # shaded by bathymetry

# Shade the triangles by a fetched value. Join spatially rather than by
# position: accessFVCOM() returns only the points inside the box, so its row
# order does not correspond to the mesh's element numbering.
currents <- accessFVCOM(vars = "UBAR", years = 2010, months = 6,
                        bounding_box = bb)
shaded <- sf::st_join(mesh, currents["UBAR"], join = sf::st_contains)
plot(shaded["UBAR"], border = NA)
}
}
\seealso{
\code{\link[=accessFVCOM]{accessFVCOM()}} for values on the mesh, \code{\link[=fvcom_archives]{fvcom_archives()}}
}
