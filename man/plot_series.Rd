% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_series}
\alias{plot_series}
\title{Plot a variable through time}
\usage{
plot_series(env_dat, vars = NULL, fun = mean, spread = TRUE, ...)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from \code{\link[=accessEnvDat]{accessEnvDat()}}}

\item{vars}{which variables to plot; \code{NULL} uses all covariate columns. Each
gets its own panel, since covariates rarely share units.}

\item{fun}{the summary applied across cells within each time step}

\item{spread}{draw the interquartile range across cells as a band}

\item{...}{passed to \code{\link[graphics:plot]{graphics::plot()}}}
}
\value{
a data frame of the plotted series, invisibly
}
\description{
Reduces each time step to one number over the study area and plots the series,
which is how a seasonal cycle, a trend, or a step change at a product boundary
becomes visible.
}
\details{
The spatial spread is drawn behind the line as a shaded band, because a mean
alone hides the difference between a uniformly warm month and one that is warm
inshore and cold offshore.
}
\examples{
\dontrun{
env <- accessEnvDat(vars = c("SST", "MLD"), years = 2003:2017, months = 1:12,
                    bounding_box = bb)

plot_series(env)
plot_series(env, "SST", fun = max)   # the warmest cell each month
}
}
\seealso{
\code{\link[=plot_env]{plot_env()}} for one step in space, \code{\link[=plot_coverage]{plot_coverage()}}
}
