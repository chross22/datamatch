% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_matched}
\alias{plot_matched}
\title{Plot observations coloured by a matched covariate}
\usage{
plot_matched(matched, var = NULL, palette = "viridis", main = NULL, ...)
}
\arguments{
\item{matched}{an \code{sf} object from \code{\link[=matchData]{matchData()}}}

\item{var}{which matched variable to colour by; \code{NULL} uses the first covariate}

\item{palette}{a \code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette name}

\item{main}{plot title}

\item{...}{passed to \code{\link[graphics:plot]{graphics::plot()}}}
}
\value{
the plotted values, invisibly
}
\description{
What \code{\link[=matchData]{matchData()}} produced, seen rather than summarised. Points that came back
\code{NA} are drawn as open circles rather than dropped, since a cluster of them is
usually the real finding — observations outside the environmental data's extent
or in a period it does not cover.
}
\section{Subset to one period first}{

The colour scale spans every observation passed in, so plotting several months
of a seasonal variable at once mixes the seasons together and the map reads as
noise: a warm February inshore point and a cool August offshore one can take
the same colour. Subset to a period before plotting, and the spatial pattern
reappears:

\if{html}{\out{<div class="sourceCode">}}\preformatted{plot_matched(matched[matched$MONTH == 7, ], "SST")
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
matched <- matchData(observations, env)

plot_matched(matched, "SST")
# Open circles are observations that matched nothing.
}
}
\seealso{
\code{\link[=matchData]{matchData()}}, \code{\link[=plot_env]{plot_env()}}
}
