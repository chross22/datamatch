% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessEnvDat.R
\name{read_day_deepest}
\alias{read_day_deepest}
\title{Read one time step's full depth column, keeping the deepest wet level}
\usage{
read_day_deepest(item, code, name)
}
\arguments{
\item{item}{\if{html}{\out{<list>}} one work item, as built by \code{\link[=accessEnvDat]{accessEnvDat()}}}

\item{code}{\if{html}{\out{<char>}} the Copernicus code fetched, e.g. \code{so}}

\item{name}{\if{html}{\out{<char>}} what to call the result column, e.g. \code{BOTS}}
}
\value{
a data frame with the value, the depth it came from, \code{YEAR}, \code{MONTH}
and \code{DAY}
}
\description{
The reanalysis publishes temperature at the sea floor but not salinity, so a
sea-floor salinity has to be taken from the three-dimensional field: fetch the
whole column and keep, in each cell, the deepest level that holds a value.
}
\section{What "deepest wet level" means}{

A model cell is \code{NA} below the sea floor, so the last level carrying a value
is the bottom-most one the model resolves there. That is the same convention
Copernicus's own \code{bottomT} follows, which is why the two pair.

It is not the sea floor itself. Level spacing coarsens with depth — from a
metre near the surface to several hundred at abyssal depths — so in deep water
the value can sit a long way above the real bottom. The depth actually used is
returned alongside the value rather than left to be assumed, in a
\verb{<name>_depth} column.
}

\keyword{internal}
