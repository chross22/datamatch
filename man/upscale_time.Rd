% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retime.R
\name{upscale_time}
\alias{upscale_time}
\title{Aggregate environmental data onto a coarser time step}
\usage{
upscale_time(
  env_dat,
  to = c("month", "year", "day"),
  vars = NULL,
  method = "mean",
  min_coverage = 0.5,
  keep_counts = FALSE
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from any access function -
\code{\link[=accessEnvDat]{accessEnvDat()}}, \code{\link[=accessFVCOM]{accessFVCOM()}}, \code{\link[=accessHYCOM]{accessHYCOM()}}, \code{\link[=accessCCMP]{accessCCMP()}} or
\code{\link[=accessERDDAP]{accessERDDAP()}}}

\item{to}{the target period: \code{"day"}, \code{"month"}, or \code{"year"}. \code{"day"} requires
hourly input, which only the wind variables have.}

\item{vars}{columns to aggregate; \code{NULL} uses all covariate columns}

\item{method}{one of \code{"mean"}, \code{"median"}, \code{"min"}, \code{"max"}, \code{"sum"}, \code{"sd"},
\code{"mode"}; length one for all variables, or a named vector per variable}

\item{min_coverage}{fraction of the period's expected steps that must carry a
value, from 0 to 1}

\item{keep_counts}{add a \verb{<var>_n} column per variable, giving the number of
steps behind each value}
}
\value{
an \code{sf} POINT object with one row per cell per target period. Daily
output keeps its \code{YEAR}/\code{MONTH}/\code{DAY} and drops \code{HOUR}; monthly output is
stamped \code{DAY = 1}; annual output \verb{MONTH = 1, DAY = 1}, matching what
\code{\link[=accessEnvDat]{accessEnvDat()}} returns for non-daily products so that
\code{\link[=matchData]{matchData()}} reads the resolution back correctly.
}
\description{
Combines the time steps falling inside each target period into one value, so an
hourly product becomes daily means, a daily one monthly, or a monthly one
annual. The grid is untouched — every cell keeps its own series, aggregated in
place.
}
\section{Hourly to daily}{

This is the route to a daily wind field. Copernicus publishes its L4 wind
hourly and monthly and nothing between, so \code{frequency = "daily"} is refused
for the wind variables; aggregating the hourly field is how a daily mean is
produced, and doing it here rather than inside \code{\link[=accessEnvDat]{accessEnvDat()}} keeps the
choice of summary — mean wind, or the day's maximum gust — with the caller.

The \code{HOUR} column is consumed rather than carried through: it is the axis
being aggregated away. The result is stamped \code{YEAR}/\code{MONTH}/\code{DAY} like any
daily product, so \code{\link[=matchData]{matchData()}} reads it back as daily.

Note that a daily mean of wind \emph{components} is not the same as a daily mean
\emph{speed}. Averaging \code{UWND} and \code{VWND} over a day and taking the magnitude gives
the net displacement of air; averaging the speed gives how hard it blew. On a
day the wind reversed, the first is near zero and the second is not.
}

\section{Choosing a method}{

As with \code{\link[=upscale_grid]{upscale_grid()}}, the summary that belongs in a period depends on the
question:

\itemize{
\item \code{mean}, \code{median} — the typical condition over the period.
\item \code{min}, \code{max} — the extreme reached within it. The coldest month of a
year is often what determines whether something overwinters somewhere; the
annual mean at the same cell can look perfectly hospitable.
\item \code{sum} — for per-step totals, such as daily primary production summing
to a seasonal total. Meaningless for a concentration.
\item \code{sd} — variability within the period, which is a covariate in its own
right: a stable month and a volatile one can share a mean.
\item \code{mode} — the commonest value, for categorical columns. Applied to them
automatically.
}

Pass one method for everything, or a named vector to vary it by variable.
}

\section{Periods that were only partly downloaded}{

A mean over the four days of January someone happened to fetch is not a January
mean, but nothing in the number itself says so. \code{min_coverage} is the fraction
of the period's \emph{expected} steps that must carry a value — 31 for January, 12
months for a year — not the fraction of the steps that were downloaded. A
partly-fetched period therefore fails the check rather than passing it
trivially.

The default of 0.5 will return \code{NA} for periods at the edges of a request. That
is the intended behaviour; set \code{min_coverage = 0} to aggregate whatever is
present, and \code{keep_counts = TRUE} to see how many steps were behind each value.
}

\examples{
\dontrun{
daily <- accessEnvDat(vars = "SST", years = 2010, months = 1:12,
                      dataset_id = "cmems_mod_glo_phy_my_0.083deg_P1D-m",
                      bounding_box = bb)

monthly <- upscale_time(daily, to = "month")

# The coldest day in each month, which the monthly mean hides
coldest <- upscale_time(daily, to = "month", method = "min")
}
}
\seealso{
\code{\link[=downscale_time]{downscale_time()}} for the other direction, \code{\link[=upscale_grid]{upscale_grid()}} for the
spatial equivalent
}
