% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bathymetry.R
\name{attach_bathymetry}
\alias{attach_bathymetry}
\title{Attach static bathymetry to a table of points}
\usage{
attach_bathymetry(dat, bathy, vars = NULL, coords = c("lon", "lat"))
}
\arguments{
\item{dat}{a data frame with coordinate columns, or an \code{sf} POINT object}

\item{bathy}{a \code{SpatRaster} from \code{fetch_bathymetry()}}

\item{vars}{which layers to attach; \code{NULL} uses all of them}

\item{coords}{names of the longitude and latitude columns, ignored for \code{sf}
input}
}
\value{
\code{dat} with one column per requested bathymetry variable
}
\description{
Bathymetry does not vary in time, so the same value is attached to every
observation at a location regardless of its date.
}
\details{
Accepts either a plain data frame with coordinate columns or an \code{sf} POINT
object, so it works on observations and on \code{accessEnvDat()} output alike.
}
\examples{
\dontrun{
bathy <- fetch_bathymetry(list(xmin = -70, xmax = -66, ymin = 41, ymax = 44))
observations <- attach_bathymetry(observations, bathy, c("DEPTH", "SLOPE"))
}
}
