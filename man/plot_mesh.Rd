% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_mesh}
\alias{plot_mesh}
\title{Map covariates on an unstructured mesh, or anywhere else}
\usage{
plot_mesh(
  x,
  var = NULL,
  values = NULL,
  time = 1,
  palette = "viridis",
  border = NA,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{an \code{sf} object with polygon or point geometry — typically from
\code{\link[=fvcom_mesh]{fvcom_mesh()}}, but any \code{sf} object works}

\item{var}{which column to shade by; \code{NULL} draws the bare geometry}

\item{values}{optional \code{sf} object holding the values, from \code{\link[=accessFVCOM]{accessFVCOM()}}
or any access function. Joined to \code{x} spatially.}

\item{time}{which time step of \code{values} to use, as an index or a named vector
of \code{YEAR}/\code{MONTH}/\code{DAY}. Only needed when \code{values} carries more than one.}

\item{palette}{a \code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette name}

\item{border}{colour for cell edges. \code{NA} hides them, which is what you want
on a fine mesh where the edges would otherwise be all you see.}

\item{main}{plot title}

\item{...}{passed to \code{\link[=plot]{plot()}}}
}
\value{
the \code{sf} object that was drawn, invisibly, with the joined column if
\code{values} was given
}
\description{
\code{\link[=plot_env]{plot_env()}} rasterises, which is right for a regular grid and wrong for an
FVCOM mesh: a triangulation has no rows and columns to rasterise onto, so the
result is either blocky or interpolated over cells the model does not have.
This draws the geometry it is given.
}
\section{What it is for}{

Two jobs. With a mesh from \code{\link[=fvcom_mesh]{fvcom_mesh()}} and nothing else, it draws the grid
itself — which is how you see where a model resolves a shelf finely and where
it does not. With \code{var} naming a column, it shades each cell by that value.
}

\section{Shading a mesh with fetched values}{

\code{\link[=accessFVCOM]{accessFVCOM()}} returns points and \code{\link[=fvcom_mesh]{fvcom_mesh()}} returns triangles, so the
two are joined spatially rather than by position — a fetch is subset to the
bounding box, so its row order says nothing about the mesh's own numbering.
Pass \code{values} and that join is done for you:

\if{html}{\out{<div class="sourceCode">}}\preformatted{mesh <- fvcom_mesh(bounding_box = bb)
sst  <- accessFVCOM(vars = "SST", years = 2010, months = 6, bounding_box = bb)
plot_mesh(mesh, "SST", values = sst)
}\if{html}{\out{</div>}}

Element-centred values (\code{UO}, \code{UBAR}, \code{TAUX}) sit at the centroid, one per
triangle. Node-centred ones (\code{SST}, \code{BOTS}) sit at the \emph{corners}, shared
between the triangles meeting there, so each triangle takes the mean of its
three — which is why the join tests intersection rather than containment: a
corner is on the boundary, and nothing contains it.
}

\examples{
\dontrun{
bb <- list(xmin = -70, xmax = -66, ymin = 41, ymax = 44)
mesh <- fvcom_mesh(bounding_box = bb)

plot_mesh(mesh)                      # the grid itself
plot_mesh(mesh, "DEPTH")             # shaded by bathymetry

sst <- accessFVCOM(vars = "SST", years = 2010, months = 6, bounding_box = bb)
plot_mesh(mesh, "SST", values = sst)
}
}
\seealso{
\code{\link[=fvcom_mesh]{fvcom_mesh()}} for the grid, \code{\link[=plot_env]{plot_env()}} for regular grids
}
