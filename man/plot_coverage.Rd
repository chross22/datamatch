% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_coverage}
\alias{plot_coverage}
\title{Plot how much data each time step actually has}
\usage{
plot_coverage(env_dat, vars = NULL, main = "Data coverage by time step", ...)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from \code{\link[=accessEnvDat]{accessEnvDat()}}}

\item{vars}{which variables to show; \code{NULL} uses all covariate columns}

\item{main}{plot title}

\item{...}{passed to \code{\link[graphics:plot]{graphics::plot()}}}
}
\value{
a data frame of the plotted coverage, invisibly, with one row per time
step per variable
}
\description{
Satellite ocean colour is missing wherever cloud, ice, or low sun angle blocked
the view, and those gaps are not spread evenly — they cluster in particular
seasons and latitudes. A series can look complete in a table and be mostly
empty in winter.
}
\details{
This plots the fraction of cells carrying a value in each time step, which is
the thing to look at before trusting a monthly mean or deciding whether
\code{\link[=fill_satellite_gaps]{fill_satellite_gaps()}} is worth the seam it introduces.
}
\examples{
\dontrun{
chl <- accessEnvDat(vars = "CHL", years = 2010, months = 1:12, bounding_box = bb)

plot_coverage(chl)
# The winter months are the ones to be careful with.
}
}
\seealso{
\code{\link[=fill_satellite_gaps]{fill_satellite_gaps()}}, \code{\link[=upscale_time]{upscale_time()}}, whose \code{min_coverage}
argument acts on the same quantity
}
