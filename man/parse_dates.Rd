% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessEnvDat.R
\name{parse_dates}
\alias{parse_dates}
\title{Parse the \code{dates} argument into a sorted, unique vector of dates}
\usage{
parse_dates(dates)
}
\arguments{
\item{dates}{the \code{dates} argument as the caller wrote it}
}
\value{
a sorted, deduplicated \code{Date} vector
}
\description{
\code{YYYYMMDD} is what a survey database usually holds, and \code{YYYY-MM-DD} is what
R prints, so both are accepted, along with \code{Date} objects and a mixture of
the three. Numbers are taken as \code{YYYYMMDD} too, since an unquoted \code{20150402}
is an easy thing to type.
}
\section{Why invalid dates are an error}{

\code{"20150230"} parses to \code{NA} rather than to a date. Dropping it would fetch
fewer days than were asked for and say nothing, leaving the gap to be found
much later as a missing row. The offending values are named instead.
}

\keyword{internal}
