% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regrid.R
\name{grid_resolution}
\alias{grid_resolution}
\title{Grid resolution of an environmental data object}
\usage{
grid_resolution(env_dat)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from any access function -
\code{\link[=accessCopernicus]{accessCopernicus()}}, \code{\link[=accessFVCOM]{accessFVCOM()}}, \code{\link[=accessHYCOM]{accessHYCOM()}}, \code{\link[=accessCCMP]{accessCCMP()}} or
\code{\link[=accessERDDAP]{accessERDDAP()}}}
}
\value{
named numeric vector \code{c(x = , y = )} in the units of the object's CRS,
degrees for every access function here. \code{NA} in a direction with only one
distinct coordinate, and meaningless on an unstructured FVCOM mesh, which
has no regular spacing to report.
}
\description{
Read off the coordinates actually present, as the median spacing between
neighbouring cell centres in each direction. The median rather than the mean
so that a single missing row or column does not shift the answer.
}
\details{
Useful on its own for deciding which way to resample: compare two products and
the coarser one is the one to bring the other onto, or not, depending on which
trade-off you want. See \code{\link[=upscale_grid]{upscale_grid()}} for what that choice costs.
}
\examples{
\dontrun{
grid_resolution(env)          # x 0.083  y 0.083
}
}
\seealso{
\code{\link[=upscale_grid]{upscale_grid()}}, \code{\link[=downscale_grid]{downscale_grid()}}
}
