% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_indices.R
\name{cached_index_file}
\alias{cached_index_file}
\title{Download an index file, reusing a recent copy}
\usage{
cached_index_file(url, index, entry, max_age = NULL, refresh = FALSE)
}
\arguments{
\item{url}{where to download from}

\item{index}{the index name, used for the cache path and messages}

\item{entry}{the catalog entry, for its \code{updates} cadence}

\item{max_age}{maximum cache age in days; \code{NULL} uses the cadence default}

\item{refresh}{re-download even if a fresh copy exists}
}
\value{
path to a local file
}
\description{
These files are re-read constantly and change slowly, so they are cached. The
hazard in caching a \emph{living} dataset is that it silently stops being current,
so the cache expires on an interval matched to how often the provider
actually publishes.
}
\keyword{internal}
