% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cefi.R
\name{cefi_file_for}
\alias{cefi_file_for}
\title{The file in a CEFI directory that holds one variable}
\usage{
cefi_file_for(files, variable, init = NULL)
}
\arguments{
\item{files}{\if{html}{\out{<char>}} filenames, as \code{\link[=cefi_catalog_files]{cefi_catalog_files()}} returns}

\item{variable}{\if{html}{\out{<char>}} the MOM6 variable name}

\item{init}{\if{html}{\out{<char>}} an initialisation tag such as \code{"i198001"}, for the
forecast archives that publish one file per initialisation; \code{NULL} for the
hindcast, which publishes one file per variable}
}
\value{
\if{html}{\out{<char>}} one filename, or \code{NA} if the variable is not published here
}
\description{
Matched on the leading segment of the name, which is the variable, so \code{tos}
does not also match \code{tossq} — the squared field beside it, which is a
different quantity and would read without complaint.
}
\keyword{internal}
