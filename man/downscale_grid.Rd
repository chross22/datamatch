% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regrid.R
\name{downscale_grid}
\alias{downscale_grid}
\title{Interpolate environmental data onto a finer grid}
\usage{
downscale_grid(
  env_dat,
  to,
  vars = NULL,
  method = "nearest",
  idw_radius = NULL,
  idw_power = 2
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object on a regular grid, from \code{\link[=accessEnvDat]{accessEnvDat()}},
\code{\link[=accessHYCOM]{accessHYCOM()}}, \code{\link[=accessCCMP]{accessCCMP()}} or \code{\link[=accessERDDAP]{accessERDDAP()}}. Not \code{\link[=accessFVCOM]{accessFVCOM()}}:
its mesh is unstructured, so there are no rows and columns to aggregate
over - match it to points, or regrid a regular product onto it.}

\item{to}{the target grid: either a resolution in the CRS's units (one number
for square cells, or \code{c(x, y)}), or another \code{sf} object whose grid to adopt —
which is the form that puts two products onto one grid.}

\item{vars}{columns to resample; \code{NULL} uses all covariate columns, as
\code{\link[=covariate_columns]{covariate_columns()}} reports them}

\item{method}{one of \code{"nearest"}, \code{"bilinear"}, \code{"cubic"}, \code{"idw"}; length one
for all variables, or a named vector per variable}

\item{idw_radius}{search radius for \code{method = "idw"}, in the CRS's units.
Defaults to twice the source cell size, which reaches the immediate
neighbours; larger draws from further away and smooths more.}

\item{idw_power}{distance exponent for \code{method = "idw"}. Higher concentrates
weight on the nearest points, approaching \code{nearest} in the limit.}
}
\value{
an \code{sf} POINT object on the target grid, with the same
\code{YEAR}/\code{MONTH}/\code{DAY} columns as the input
}
\description{
Puts a coarse field onto a finer grid, so a 0.25 degree model variable can join
4 km satellite data without the satellite being coarsened to meet it.
}
\section{What this does and does not do}{

Downscaling adds cells, not information. A 0.25 degree field rendered at 4 km
has 4 km \emph{cells}, but it still resolves nothing below 0.25 degrees, and no
method here changes that — none of them consult any other data source, so there
is nowhere for real fine-scale structure to come from.

That distinction is easy to lose downstream, which is why \code{nearest} is the
default. It replicates each coarse value across the fine cells inside it, so
the result is visibly blocky at the source resolution: the output looks like
what it is. \code{bilinear} and \code{cubic} return a smooth field that looks like a
finely-resolved measurement and is not one. They are the better choice when
the field really is smooth at the source scale and blockiness would be an
artefact — sea surface height, say — and the worse choice when someone later
computes a gradient from the result, because that gradient is then a property
of the interpolator.

\itemize{
\item \code{nearest} — the value of the containing coarse cell. Invents nothing;
blocky. Required for categorical fields, and applied to them automatically.
\item \code{bilinear} — weighted by the four surrounding cell centres. Smooth, and
\code{NA} anywhere a contributing neighbour is \code{NA}, so gaps grow by a cell.
\item \code{cubic} — smoother again, over sixteen cells. Can overshoot past the
source range near sharp edges, which for a bounded quantity like
chlorophyll can produce negatives.
\item \code{idw} — inverse distance weighting over the source points within
\code{idw_radius}. Unlike the others it fills across holes rather than
propagating them, which makes it the one to use on gappy satellite data
where \code{\link[=fill_satellite_gaps]{fill_satellite_gaps()}} is not an option.
}
}

\examples{
\dontrun{
# Model chlorophyll (0.25 deg) onto the satellite grid, blockiness intact
fine <- downscale_grid(chl_model, to = chl_satellite)

# Smooth, for a field that really is smooth at the source scale
ssh <- downscale_grid(ssh_model, to = 0.04, method = "bilinear")
}
}
\seealso{
\code{\link[=upscale_grid]{upscale_grid()}} for the other direction, \code{\link[=fill_satellite_gaps]{fill_satellite_gaps()}}
}
