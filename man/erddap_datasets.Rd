% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erddap.R
\name{erddap_datasets}
\alias{erddap_datasets}
\title{Satellite datasets read through ERDDAP}
\usage{
erddap_datasets()
}
\value{
a named list, one entry per dataset, each with \code{server},
\code{dataset_id}, \code{variables}, \code{has_altitude}, \code{start}, \code{end}, \code{resolution},
\code{label} and \code{reference}
}
\description{
ERDDAP is NOAA's data server, and several satellite products this package
wants are on one. It matters here for a practical reason: the same products
at PO.DAAC need an Earthdata login, and ERDDAP serves them with \strong{no account
at all}, subsetting server-side by longitude, latitude and time.
}
\section{What is built in, and what is not}{

Three datasets ship. They are the ones that are global, current, and relevant
to a shelf study:

\tabular{lll}{
\code{MUR}          \tab SST, 0.01 deg daily  \tab 2002-06 onward \cr
\code{VIIRSCHL}     \tab chlorophyll, daily   \tab 2020-05 onward \cr
\code{VIIRSCHL2018} \tab chlorophyll, daily   \tab 2012-01 to 2022-07
}

\strong{There is no global VIIRS SST here.} The VIIRS SST dataset on these servers
covers the US West Coast only (-128 to -115 E), which is no use on the
Atlantic shelf, and nothing global turned up in its place. MUR is the SST to
use instead — it is a blended analysis that takes VIIRS among its inputs, so
it carries the same sensor's information at higher resolution and over a much
longer record.

The two chlorophyll entries are the same instrument processed twice.
\code{VIIRSCHL2018} is the 2018 reprocessing, which stops in July 2022;
\code{VIIRSCHL} is the current gap-filled product, which starts in May 2020. They
overlap for two years and are not the same numbers, so a series spanning both
has a seam in it.
}

\section{MUR is not a model SST}{

\code{MUR} is a satellite analysis of the \emph{foundation} temperature — the
temperature below the daily warming layer — where a model \code{SST} is the
topmost model level. On a calm sunny afternoon those differ by a degree or
more. Both arrive in a column called \code{SST}, and \code{\link[=source_of]{source_of()}} is what
distinguishes them.
}

\examples{
names(erddap_datasets())
erddap_datasets()$MUR$dataset_id
}
\seealso{
\code{\link[=accessERDDAP]{accessERDDAP()}}, \code{\link[=erddap_dataset]{erddap_dataset()}} for any other ERDDAP dataset
}
