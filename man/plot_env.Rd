% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_env}
\alias{plot_env}
\title{Map an environmental variable}
\usage{
plot_env(env_dat, var = NULL, time = 1, palette = "viridis", main = NULL, ...)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object from any access function -
\code{\link[=accessEnvDat]{accessEnvDat()}}, \code{\link[=accessFVCOM]{accessFVCOM()}}, \code{\link[=accessHYCOM]{accessHYCOM()}}, \code{\link[=accessCCMP]{accessCCMP()}} or
\code{\link[=accessERDDAP]{accessERDDAP()}}}

\item{var}{which variable to map; \code{NULL} uses the first covariate column}

\item{time}{which time step, as an index or a named vector of
\code{YEAR}/\code{MONTH}/\code{DAY} values}

\item{palette}{a \code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette name. The default is
perceptually uniform and readable in greyscale, which matters more for a
field of continuous values than the usual rainbow does.}

\item{main}{plot title; the default names the variable and the time step}

\item{...}{passed to \code{\link[terra:plot]{terra::plot()}}}
}
\value{
the \code{SpatRaster} that was plotted, invisibly
}
\description{
A quick look at what came back from any access function — the first thing worth
doing after a download, and the fastest way to catch a bounding box that came
out somewhere unintended, a variable that is all \code{NA}, or a depth range that
returned the wrong level.
}
\details{
The data are rendered as a raster rather than as points, so gaps read as holes
rather than as absent dots, and cell size is visible.
}
\section{Time steps}{

Environmental data carries many time steps and a map shows one. \code{time} picks
it: a number is an index into the steps present, and a list or vector names
the step directly (\code{c(YEAR = 2010, MONTH = 6)}). The default is the first step,
and the step being shown is written into the title so a map is never ambiguous
about which month it is.
}

\examples{
\dontrun{
env <- accessEnvDat(vars = c("SST", "CHL"), years = 2010, months = 1:12,
                    bounding_box = bb)

plot_env(env)                                  # first variable, first step
plot_env(env, "CHL", time = c(MONTH = 6))      # June chlorophyll
}
}
\seealso{
\code{\link[=plot_coverage]{plot_coverage()}} for where the gaps are, \code{\link[=plot_series]{plot_series()}} for how a
variable moves through time
}
