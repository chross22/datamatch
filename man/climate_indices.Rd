% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_indices.R
\name{climate_indices}
\alias{climate_indices}
\title{Catalog of basin-scale climate indices}
\usage{
climate_indices()
}
\value{
a named list, one entry per index, each with \code{label}, \code{units},
\code{source}, \code{url}, and \code{description}
}
\description{
Unlike the gridded variables, these have \strong{no spatial dimension} — one value
per month describes the whole basin. They enter a model as a shared state
every observation in a month experiences, which is a different kind of
covariate from local temperature: they carry no information about \emph{where}
within a region conditions are better, only about what year and season it was.
}
\details{
That makes them useful for interannual questions ("was this a warm-regime
year?") and useless for spatial ones. A model given only indices cannot
produce a map.
}
\examples{
names(climate_indices())
}
\seealso{
\code{\link[=fetch_climate_index]{fetch_climate_index()}}, \code{\link[=attach_climate_index]{attach_climate_index()}}
}
