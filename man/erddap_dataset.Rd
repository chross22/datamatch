% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/erddap.R
\name{erddap_dataset}
\alias{erddap_dataset}
\title{Describe any ERDDAP griddap dataset, so it can be read like a built-in one}
\usage{
erddap_dataset(
  server,
  dataset_id,
  variables = NULL,
  label = NULL,
  reference = NA_character_
)
}
\arguments{
\item{server}{\if{html}{\out{<char>}} the ERDDAP base URL, e.g.
\code{"https://coastwatch.pfeg.noaa.gov/erddap"}}

\item{dataset_id}{\if{html}{\out{<char>}} the griddap dataset id}

\item{variables}{\if{html}{\out{<char>}} named vector mapping the names you want to this
dataset's variable names, as \code{c(SST = "analysed_sst")}. When \code{NULL}, every
data variable is offered under its own name.}

\item{label}{\if{html}{\out{<char>}} a human-readable name; defaults to the dataset id}

\item{reference}{\if{html}{\out{<char>}} the citation for this product, if there is one}
}
\value{
a list in the shape \code{\link[=erddap_datasets]{erddap_datasets()}} entries take, ready to pass
to \code{\link[=accessERDDAP]{accessERDDAP()}} as \code{dataset}
}
\description{
ERDDAP servers host thousands of gridded datasets, and \code{\link[=erddap_datasets]{erddap_datasets()}}
ships three. This is how to reach any of the rest: give it a server and a
dataset id, and it reads the dataset's own metadata to work out the axes,
the variables and the period covered.
}
\section{What it checks}{

The dataset's \code{.das} is fetched and parsed once, so a wrong id, an
unreachable server, or a dataset that is tabular rather than gridded fails
here — with the reason — rather than part-way through a fetch. Whether the
dataset carries a singleton \code{altitude} axis is detected rather than assumed,
because that changes the shape of every request.
}

\examples{
\dontrun{
mine <- erddap_dataset("https://coastwatch.pfeg.noaa.gov/erddap",
                       "jplMURSST41", variables = c(SST = "analysed_sst"))
str(mine)
}
}
\seealso{
\code{\link[=accessERDDAP]{accessERDDAP()}}, \code{\link[=erddap_datasets]{erddap_datasets()}}
}
