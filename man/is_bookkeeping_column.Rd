% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{is_bookkeeping_column}
\alias{is_bookkeeping_column}
\title{Columns that are bookkeeping rather than data}
\usage{
is_bookkeeping_column(names)
}
\arguments{
\item{names}{\if{html}{\out{<char>}} column names to inspect}
}
\value{
\if{html}{\out{<logical>}} one per name
}
\description{
\verb{<var>_depth} records which model level a derived bottom value was taken
from, and \code{.datamatch_source} is the per-row source tag a fetch spanning
several archives carries. Neither measures anything: the mean of two depths
is not the depth any value came from, and the internal tag is consumed by
\code{\link[=matchData]{matchData()}} rather than kept. So \code{\link[=covariate_columns]{covariate_columns()}} leaves both out, and
nothing resamples or plots them as though they were covariates.
}
\section{Why \verb{<var>_source} is not here}{

It is provenance too, but it travels with the variable it describes rather
than being left behind by it. \code{\link[=upscale_grid]{upscale_grid()}} and \code{\link[=upscale_time]{upscale_time()}} carry a
non-numeric column as a categorical - the commonest value when aggregating,
the nearest when interpolating - which is exactly what a source tag needs,
and both were written expecting one to ride along. Excluding it here meant it
never did, so the record of which source a value came from was lost at the
first resample, which is the point at which it matters most.
}

\keyword{internal}
