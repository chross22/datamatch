% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obdaac.R
\name{obdaac_variables}
\alias{obdaac_variables}
\title{Catalog of OB.DAAC variables under familiar names}
\usage{
obdaac_variables()
}
\value{
a named list, one entry per variable, each with \code{suite}, \code{variable},
\code{label}, \code{units} and \code{description}
}
\description{
Maps the names this package uses onto the product suite and the variable
inside it that supply them.
}
\section{Which SST this is}{

MODIS and VIIRS publish more than one sea surface temperature, and they are
different measurements rather than different names for one:

\itemize{
\item \strong{\code{SST}} is the 11 um retrieval from the daytime pass, which sees
the skin of the ocean including whatever the sun has warmed that
afternoon.
\item \strong{\code{SST_NIGHT}} is the same 11 um retrieval from the night pass,
with that diurnal warming absent. It is the one to use where a
comparison across days or sensors has to be like for like.
}

Neither is the foundation temperature that \code{MUR} in \code{\link[=erddap_datasets]{erddap_datasets()}}
analyses, and none of the three is the topmost model level that a reanalysis
calls \code{SST}. All four arrive in a column called \code{SST} unless you ask for the
night one, and \code{\link[=source_of]{source_of()}} is what tells them apart.
}

\section{Chlorophyll is not gap-free}{

These are single-sensor Level-3 composites, so a daily field is mostly cloud
outside the tropics and a \code{CHL} column from one is mostly \code{NA}. That is the
honest state of the measurement rather than a fault, and there are three
ways through it: composite in time by asking for \code{frequency = "8day"} or
\code{"monthly"}, interpolate with \code{\link[=fill_satellite_gaps]{fill_satellite_gaps()}}, which records what it
filled, or use a gap-free product — the daily Copernicus \code{CHL} and the
\code{VIIRSCHL} entry in \code{\link[=erddap_datasets]{erddap_datasets()}} are both already interpolated.
}

\examples{
names(obdaac_variables())
obdaac_variables()$CHL$suite
}
\seealso{
\code{\link[=accessOBDAAC]{accessOBDAAC()}}, \code{\link[=obdaac_dictionary]{obdaac_dictionary()}} for a printable table
}
