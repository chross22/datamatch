% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cefi.R
\name{accessCEFI}
\alias{accessCEFI}
\title{Access CEFI regional MOM6 output}
\usage{
accessCEFI(
  vars,
  years = NULL,
  months = NULL,
  bounding_box,
  dates = NULL,
  frequency = c("monthly", "daily"),
  experiment = "hindcast",
  init = NULL,
  member = NULL,
  release = "latest",
  archive = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{vars}{\if{html}{\out{<char>}} variables to read, from \code{\link[=cefi_variables]{cefi_variables()}}}

\item{years}{\if{html}{\out{<numeric>}} years to read. Required unless \code{dates} is given.}

\item{months}{\if{html}{\out{<numeric>}} months to read. Required unless \code{dates} is given.}

\item{bounding_box}{\if{html}{\out{<list>}} named list with \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}, or
an \code{sf}/\code{sfc} object. Longitudes negative west.}

\item{dates}{the exact dates to read, as \code{YYYYMMDD} strings, \code{YYYY-MM-DD}
strings, or \code{Date} objects}

\item{frequency}{\if{html}{\out{<char>}} \code{"monthly"} (the default) or \code{"daily"}}

\item{experiment}{\if{html}{\out{<char>}} which CEFI experiment to read. \code{"hindcast"} is the
default; \code{"decadal_forecast"} is experimental. The rest are refused with
the reason — see \code{\link[=cefi_experiments]{cefi_experiments()}}.}

\item{init}{\if{html}{\out{<char>}} for a forecast, which initialisation to read, as
\code{"i198001"} or \code{"198001"}. Required for \code{"decadal_forecast"}.}

\item{member}{\if{html}{\out{<integer>}} for a forecast, which ensemble member or members to
read, from 1 to 10. Required for \code{"decadal_forecast"}. Several may be
given, and each arrives as its own block of rows carrying its own source
tag.}

\item{release}{\if{html}{\out{<char>}} a release directory such as \code{"r20250715"}, or
\code{"latest"} (the default), which follows whatever CEFI published last}

\item{archive}{a spec from \code{\link[=cefi_archive]{cefi_archive()}}, to read a domain this package
does not ship. Overrides \code{experiment}, \code{frequency} and \code{release}.}

\item{overwrite}{\if{html}{\out{<logical>}} re-read time steps already cached}
}
\value{
\if{html}{\out{<sf object>}} one row per grid cell per time step, with \code{YEAR},
\code{MONTH}, \code{DAY} and a column per requested variable
}
\description{
Reads the NOAA CEFI regional ocean model over OPeNDAP and returns it as an
\code{sf} point object with one row per grid cell and time step — the same shape
\code{\link[=accessCopernicus]{accessCopernicus()}}, \code{\link[=accessFVCOM]{accessFVCOM()}}, \code{\link[=accessHYCOM]{accessHYCOM()}}, \code{\link[=accessCCMP]{accessCCMP()}} and
\code{\link[=accessERDDAP]{accessERDDAP()}} return, so \code{\link[=matchData]{matchData()}} joins it unchanged.
}
\section{Why reach for it, and why not}{

On the Northwest Atlantic shelf this is the highest-resolution coupled
physics-and-biogeochemistry field available here: a twelfth of a degree for
both, against a quarter degree for the Copernicus biogeochemical reanalysis.
It carries nutrients, oxygen, pH, pCO2, phytoplankton carbon and
mesozooplankton biomass on the same grid as the temperature and salinity,
which no other source in this package does.

Against that: \strong{it is one region}. Outside roughly 98W-36W and 5N-58N there
is nothing to read, and a bounding box outside the domain is refused rather
than returning an empty join. It is also \strong{a model throughout} — its \code{CHL}
is simulated, not retrieved, and is not the satellite \code{CHL} from
\code{\link[=accessERDDAP]{accessERDDAP()}} under another name.
}

\section{Forecasts are experimental}{

\code{experiment = "decadal_forecast"} reads a prediction rather than a
reconstruction, and this package treats it as experimental: it warns on
every call, and it will not choose for you between the things a forecast
makes you choose.

A decadal file is ten years from one January, and there are sixty of them,
so \code{init} says which initialisation to read. Each holds \strong{ten ensemble
members}, so \code{member} says which. Neither has a defensible default and
neither is guessed:

\itemize{
\item The members are not repeats of one number. They are the model's own
estimate of how uncertain it is, and averaging them is a modelling
decision — a reasonable one, often the right one, but not one a fetch
should make silently. Read the members you want and combine them
yourself, so the combination is visible in your code.
\item A year covered by several initialisations is covered by several
different forecasts of it, at different lead times. Which one you mean
is a question about your analysis, not about the archive.
}

The source tag records both, so \code{\link[=source_of]{source_of()}} on the result says which
initialisation and which member produced it.
}

\section{What is monthly and what is also daily}{

The hindcast saves everything monthly. Its \strong{daily} output is
biogeochemistry only — see the section of the same name in
\code{\link[=cefi_variables]{cefi_variables()}} — and a daily request for \code{SST} is refused with the list
of what is available daily rather than returning nothing.
}

\section{Longitude and the domain}{

The regridded files are already on a -180 to 180 grid, so \code{bounding_box} is
given negative west as everywhere else in this package and needs no
conversion in either direction.
}

\examples{
\dontrun{
bb <- list(xmin = -70, xmax = -66, ymin = 41, ymax = 44)

# The hindcast, monthly
env <- accessCEFI(vars = c("SST", "BOTT", "BOTS"), years = 2015,
                  months = 1:12, bounding_box = bb)
source_of(env)
#> [1] "cefi:NWA12-hindcast-r20250715"

# Daily biogeochemistry on survey dates
bgc <- accessCEFI(vars = c("CHL", "NO3", "PH"), frequency = "daily",
                  dates = unique(observations$date), bounding_box = bb)

matched <- matchData(observations, bgc)
}
}
\seealso{
\code{\link[=cefi_variables]{cefi_variables()}}, \code{\link[=cefi_archives]{cefi_archives()}}, \code{\link[=cefi_archive]{cefi_archive()}},
\code{\link[=accessCopernicus]{accessCopernicus()}} for a global reanalysis to compare against
}
