% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obdaac.R
\name{obdaac_filename}
\alias{obdaac_filename}
\title{The OB.DAAC filename that holds one variable for one period}
\usage{
obdaac_filename(sensor, entry, when, frequency, resolution)
}
\arguments{
\item{sensor}{one entry of \code{\link[=obdaac_sensors]{obdaac_sensors()}}}

\item{entry}{one entry of \code{\link[=obdaac_variables]{obdaac_variables()}}}

\item{when}{\if{html}{\out{<Date>}} the day, or the first of the month for a composite}

\item{frequency}{\if{html}{\out{<char>}} \code{"daily"} or \code{"monthly"}}

\item{resolution}{\if{html}{\out{<char>}} \code{"4km"} or \code{"9km"}}
}
\value{
\if{html}{\out{<char>}} the filename
}
\description{
The OB.DAAC filename that holds one variable for one period
}
\section{Why the name is constructed rather than looked up}{

OB.DAAC has a file search that would answer this exactly, report each file's
size, and know which days a sensor missed. It accepts \strong{POST only}, and
\code{utils::download.file()} cannot POST, so using it would mean adding an HTTP
package to reach one endpoint.

The names are worth constructing instead because they are strictly regular
across every sensor and suite here — verified against the archive for
SeaWiFS, both MODIS instruments and all three VIIRS, for daily and monthly,
for the colour suites and the temperature ones. The one mission whose names
are \emph{not} constructible is PACE, and it is left out for that reason among
others; see \code{\link[=obdaac_sensors]{obdaac_sensors()}}.

What is lost is the ability to know, before trying, that a sensor returned
nothing on a given day. That surfaces as a failed download for that step
instead, which \code{\link[=accessOBDAAC]{accessOBDAAC()}} reports by name and carries on past.
}

\keyword{internal}
