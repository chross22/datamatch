% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cefi.R
\name{cefi_read_variable}
\alias{cefi_read_variable}
\title{Read the requested time steps of one CEFI variable}
\usage{
cefi_read_variable(
  handle,
  entry,
  index,
  keep_lon,
  keep_lat,
  member = NA_integer_
)
}
\arguments{
\item{handle}{an open \code{ncdf4} handle}

\item{entry}{one entry of \code{\link[=cefi_variables]{cefi_variables()}}}

\item{index}{\if{html}{\out{<integer>}} the axis indices to read}

\item{keep_lon, keep_lat}{\if{html}{\out{<integer>}} the grid indices the bounding box selects}

\item{member}{\if{html}{\out{<integer>}} which ensemble member, or \code{NA} for a single run}
}
\value{
a named list of numeric vectors, one per requested index, each in
\code{expand.grid(lon, lat)} order
}
\description{
Read the requested time steps of one CEFI variable
}
\section{Why runs rather than steps or spans}{

Reading each wanted step with its own request is one network round trip per
month, which for a decade is 120 of them. Reading everything between the
first and the last transfers the whole decade to keep ten months of it.
Contiguous runs are neither: a stretch of consecutive months is one request,
and a gap ends the run instead of being read across. See
\code{\link[=contiguous_runs]{contiguous_runs()}}.
}

\keyword{internal}
