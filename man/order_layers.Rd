% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessEnvDat.R
\name{order_layers}
\alias{order_layers}
\title{Put a downloaded raster's layers into the order they were requested}
\usage{
order_layers(x, vars)
}
\arguments{
\item{x}{a \code{SpatRaster} read from a Copernicus download}

\item{vars}{\if{html}{\out{<char>}} variable codes, in the order they were requested}
}
\value{
\code{x}, with one layer per requested code, in that order
}
\description{
Copernicus returns layers in the NetCDF's own order, which is alphabetical by
variable code and has nothing to do with the order they were asked for. Column
names are assigned from \code{vars}, so the two have to be reconciled before naming
or the names land on the wrong layers.
}
\details{
That failure is silent and severe: requesting \code{c("SST", "SSS")} sends
\code{c("thetao", "so")}, gets back \code{so} then \code{thetao}, and would label salinity as
temperature and temperature as salinity with nothing to indicate it. Matching
by name rather than position is the whole point of this function.
}
\section{Layer names}{

Three-dimensional variables come back as \code{thetao_depth=0.494025}, so the depth
suffix is stripped before matching. Two-dimensional ones such as \code{mlotst} and
\code{zos} carry no suffix.

A code appearing on more than one layer means the depth range spanned several
model levels. That is reported as such, rather than left to be caught later by
a column count that cannot say which variable caused it.
}

\keyword{internal}
