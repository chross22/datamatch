% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessEnvDat.R
\name{accessEnvDat}
\alias{accessEnvDat}
\title{Access environmental data from Copernicus Marine Service}
\usage{
accessEnvDat(
  product_id = NULL,
  dataset_id = NULL,
  vars,
  years = NULL,
  months = NULL,
  bounding_box,
  depth = c(0, 1),
  overwrite = FALSE,
  n_workers = 4,
  frequency = c("monthly", "daily"),
  dates = NULL,
  mode = c("reanalysis", "forecast")
)
}
\arguments{
\item{product_id}{\if{html}{\out{<char>}} product identification string from the Copernicus
Marine Data Store. Optional when \code{vars} are catalog names.}

\item{dataset_id}{\if{html}{\out{<char>}} dataset identification string from the Copernicus
Marine Data Store. Optional when \code{vars} are catalog names.}

\item{vars}{\if{html}{\out{<char>}} variables to access: names from \code{\link[=variable_dictionary]{variable_dictionary()}},
raw Copernicus variable codes, or a mixture}

\item{years}{\if{html}{\out{<numeric>}} years of data to access. Required unless \code{dates} is
given, which names the time steps itself.}

\item{months}{\if{html}{\out{<numeric>}} months of data to access. Required unless \code{dates} is
given.}

\item{bounding_box}{\if{html}{\out{<list>}} named list of spatial coordinates of bounding box}

\item{depth}{\if{html}{\out{<numeric>}} depth range to access (in meters)}

\item{overwrite}{\if{html}{\out{<logical>}} whether or not to overwrite the data if it exists locally}

\item{n_workers}{\if{html}{\out{<integer>}} how many days to download at once. See the
Downloading in parallel section. Use \code{n_workers = 1} to download one day at
a time.}

\item{frequency}{\if{html}{\out{<char>}} \code{"monthly"} (the default) for monthly means, or
\code{"daily"} for daily ones. See the Monthly and daily data section. Ignored
when \code{dataset_id} is given, since the dataset itself fixes the step.}

\item{dates}{the exact dates to fetch, as \code{YYYYMMDD} strings,
\code{YYYY-MM-DD} strings, or \code{Date} objects. \code{NULL}, the default, fetches every
day of the requested months instead. Passing \code{dates} implies
\code{frequency = "daily"} and replaces \code{years} and \code{months}, which must then not
be given. See the Fetching specific dates section.}

\item{mode}{\if{html}{\out{<char>}} \code{"reanalysis"} (the default) for the multi-year hindcast,
or \code{"forecast"} for the analysis-and-forecast products, which run to about
ten days ahead. See \code{\link[=forecast_variables]{forecast_variables()}} for which variables have a
forecast equivalent and how the identifiers differ.}
}
\value{
\if{html}{\out{<sf object>}} sf object containing requested environmental data from Copernicus Marine Service
}
\description{
Downloads a Copernicus dataset over a bounding box and time range, and returns
it as an \code{sf} point object with one row per grid cell and time step.
}
\section{Requesting variables by name}{

\code{vars} accepts the short names in \code{\link[=variable_dictionary]{variable_dictionary()}} — \code{"SST"}, \code{"CHL"},
\code{"MLD"} — as well as raw Copernicus codes. Copernicus codes are terse and easy
to misremember (\code{thetao} for temperature, \code{mlotst} for mixed layer depth,
\code{zos} for sea surface height), and getting one wrong produces a failed
download rather than an obvious mistake.

Names carry through to the result, so a request for \code{"SST"} returns a column
called \code{SST} rather than \code{thetao}.

Because the catalog knows which product and dataset holds each variable,
\strong{\code{product_id} and \code{dataset_id} can be omitted} when every requested variable
is in it:

\if{html}{\out{<div class="sourceCode">}}\preformatted{accessEnvDat(
  vars = c("SST", "SSS", "MLD"),
  years = 2003:2017, months = 1:12,
  bounding_box = list(xmin = -76, xmax = -65, ymin = 35, ymax = 45)
)
}\if{html}{\out{</div>}}

Variables from different datasets cannot be fetched in one request, and mixing
them is refused before anything is downloaded rather than failing obscurely at
the API. Anything outside the catalog is passed through as a code, with a
warning — Copernicus serves far more than the catalog covers, but a typo looks
identical to a real code.
}

\section{Monthly and daily data}{

\code{frequency = "monthly"} (the default) fetches monthly means: one field per
month, and one row per grid cell per month. \code{frequency = "daily"} fetches the
daily datasets instead, expanding each requested month into its days.

\if{html}{\out{<div class="sourceCode">}}\preformatted{accessEnvDat(vars = c("SST", "MLD"), frequency = "daily",
             years = 2015, months = 4:6,
             bounding_box = list(xmin = -70, xmax = -65, ymin = 42, ymax = 45))
}\if{html}{\out{</div>}}

Note what that costs: three months of daily data is 91 downloads rather than
3, and 91 grids rather than 3 in memory. A decade of daily data over a large
box will not fit in a laptop's RAM as an \code{sf} object, and is better fetched a
season at a time.
}

\section{Fetching specific dates}{

\code{dates} is the other way to keep that in hand. It names the exact dates to
fetch, so only the days that matter are downloaded:

\if{html}{\out{<div class="sourceCode">}}\preformatted{accessEnvDat(vars = "SST", dates = c("20150402", "20150517", "20150623"),
             bounding_box = bb)
}\if{html}{\out{</div>}}

This is the argument to use when matching daily data to observations. Survey
dates differ from month to month, and a rule such as "the 1st and 15th" does
not describe them. Take the dates from the observations themselves:

\if{html}{\out{<div class="sourceCode">}}\preformatted{accessEnvDat(vars = "SST", dates = unique(observations$date), bounding_box = bb)
}\if{html}{\out{</div>}}

\code{YYYYMMDD} strings, \code{YYYY-MM-DD} strings, and \code{Date} objects are all accepted,
and may be mixed. Dates are sorted and deduplicated, so the result comes back
in date order however the argument was written, and a date named twice is
fetched once.

\code{dates} says which time steps to fetch, so \code{years} and \code{months} are neither
needed nor accepted alongside it. Passing it implies \code{frequency = "daily"},
since naming a date means nothing to a monthly mean, and passing it with an
explicit \code{frequency = "monthly"} is a contradiction rather than something to
resolve by guessing.

A date that does not exist — \code{"20150230"} — is an error naming it, rather than
a silently dropped request.

Fetching dates is not the same as averaging a month. Three dates are a sample
of the month, with whatever weather fell on them; a monthly mean is the month.
Which you want depends on whether the observations being matched are themselves
instants or aggregates.

Not every variable has a daily equivalent. \code{PH}, \code{PP}, \code{DIATO} and \code{DINO} are
published as monthly composites only, and asking for them daily is refused
before anything is downloaded. Daily \code{CHL} comes from the gap-free
interpolated ocean colour dataset rather than the monthly composite, which
\code{\link[=accessEnvDat]{accessEnvDat()}} reports when it happens — see \code{\link[=copernicus_variables]{copernicus_variables()}}.

Passing \code{dataset_id} explicitly overrides all of this: that dataset's own
frequency decides, since a Copernicus dataset is published at one step.
}

\section{Downloading in parallel}{

Days already in the cache are read directly. Only the missing ones are
downloaded, and those go out \code{n_workers} at a time through a PSOCK cluster,
since a Copernicus subset request spends nearly all its time waiting on the
API rather than on this machine.

The default of 4 is deliberately modest. The limit is the service and the
network, not local cores, and a large \code{n_workers} mostly earns rate limiting.
Raise it toward 8 for many small requests; use \code{n_workers = 1} to download
serially.

The cluster is PSOCK rather than fork-based (\code{\link[parallel:mclapply]{parallel::mclapply()}}): GDAL,
which \code{terra} and \code{sf} use internally, is not fork-safe, and forking after it
has initialised can corrupt state in the children. PSOCK workers are fresh R
sessions, which avoids that.

Reading and converting the downloaded files stays in this session. Those are
local file reads, and returning each day's data frame from a worker would
cost more in serialisation than the read saves.

A day that fails does not abort the others. Every day is attempted, the
successful ones stay in the cache, and the error names each day that failed —
so re-running the same call retries only those.
}

