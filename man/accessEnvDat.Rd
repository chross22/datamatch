% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copernicus.R
\name{accessEnvDat}
\alias{accessEnvDat}
\title{Fetch Copernicus data (deprecated name)}
\usage{
accessEnvDat(...)
}
\arguments{
\item{...}{passed to \code{\link[=accessCopernicus]{accessCopernicus()}}}
}
\value{
whatever \code{\link[=accessCopernicus]{accessCopernicus()}} returns
}
\description{
\code{accessEnvDat()} is the old name for \code{\link[=accessCopernicus]{accessCopernicus()}}. It still works and
warns; it will be removed in a later version.
}
\section{Why it was renamed}{

The name dates from when Copernicus was the only source. With five, "access
environmental data" reads as though it fetches from all of them, sitting
beside \code{\link[=accessFVCOM]{accessFVCOM()}}, \code{\link[=accessHYCOM]{accessHYCOM()}}, \code{\link[=accessCCMP]{accessCCMP()}} and \code{\link[=accessERDDAP]{accessERDDAP()}},
which each say what they read. \code{accessCopernicus()} says the same.

Nothing else changes: the arguments, the result, and the \verb{copernicus:} source
tag are all as they were.
}

\examples{
\dontrun{
# Both do the same thing; the first warns.
env <- accessEnvDat(vars = "SST", years = 2010, months = 1:12, bounding_box = bb)
env <- accessCopernicus(vars = "SST", years = 2010, months = 1:12, bounding_box = bb)
}
}
\seealso{
\code{\link[=accessCopernicus]{accessCopernicus()}}
}
