% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cefi.R
\name{cefi_archive}
\alias{cefi_archive}
\title{Describe any CEFI directory, so it can be read like a built-in one}
\usage{
cefi_archive(
  path,
  frequency = c("monthly", "daily"),
  grid = c("regrid", "raw"),
  release = "latest",
  label = NULL
)
}
\arguments{
\item{path}{\if{html}{\out{<char>}} the catalog path under the THREDDS root, as in a
\code{\link[=cefi_archives]{cefi_archives()}} entry's \code{path}}

\item{frequency}{\if{html}{\out{<char>}} \code{"monthly"} or \code{"daily"}}

\item{grid}{\if{html}{\out{<char>}} \code{"regrid"} for the regular latitude-longitude
interpolation, or \code{"raw"} for the model's own curvilinear grid. This
package reads \code{"regrid"}; \code{"raw"} will open but not read, because the
reader assumes a regular grid.}

\item{release}{\if{html}{\out{<char>}} a release directory such as \code{"r20250715"}, or
\code{"latest"}}

\item{label}{\if{html}{\out{<char>}} a human-readable name; defaults to the path}
}
\value{
a list in the shape \code{\link[=cefi_archives]{cefi_archives()}} entries take, ready to pass to
\code{\link[=accessCEFI]{accessCEFI()}} as \code{archive}
}
\description{
\code{\link[=cefi_archives]{cefi_archives()}} ships the Northwest Atlantic hindcast and its decadal
forecast, because that is the domain this package was written for. CEFI also
publishes the Northeast Pacific, the Arctic, the Pacific Islands and the
Great Lakes, on the same server in the same layout, and this is how to reach
them: give it a catalog path and it reads the directory to work out the rest.
}
\section{What it checks}{

The catalog is read once, so a path that is wrong, a release that does not
exist, or a server that is down fails here — with the reason — rather than
part-way through a fetch. The variables actually published in that directory
come back in the returned spec, so \code{str()} on it says what is there.
}

\examples{
\dontrun{
pacific <- cefi_archive(paste0("Projects/CEFI/regional_mom6/cefi_portal/",
                               "northeast_pacific/full_domain/hindcast"))
str(pacific)
}
}
\seealso{
\code{\link[=accessCEFI]{accessCEFI()}}, \code{\link[=cefi_archives]{cefi_archives()}}
}
