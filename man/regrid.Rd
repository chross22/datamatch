% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regrid.R
\name{regrid}
\alias{regrid}
\title{Resample environmental data onto another grid}
\usage{
regrid(
  env_dat,
  to,
  vars = NULL,
  method,
  direction,
  min_coverage = 0,
  keep_counts = FALSE,
  idw_radius = NULL,
  idw_power = 2
)
}
\arguments{
\item{env_dat}{an \code{sf} POINT object on a regular grid}

\item{to}{target resolution or \code{sf} object}

\item{vars}{columns to resample, or \code{NULL} for all covariates}

\item{method}{method name(s), validated against \code{direction}}

\item{direction}{\code{"up"} or \code{"down"}}

\item{min_coverage}{minimum non-missing fraction (upscaling only)}

\item{keep_counts}{add coverage columns (upscaling only)}

\item{idw_radius, idw_power}{inverse distance weighting parameters}
}
\value{
an \code{sf} POINT object on the target grid
}
\description{
The shared implementation of \code{\link[=upscale_grid]{upscale_grid()}} and \code{\link[=downscale_grid]{downscale_grid()}}. Both
directions are the same operation on a different set of methods, so they differ
only in what they validate and which terra call they end up in.
}
\details{
Time steps are handled one at a time, as \code{\link[=fill_satellite_gaps]{fill_satellite_gaps()}} does: each
step is a complete grid of its own, and stacking them into one raster would
make a variable's layers indistinguishable from its time steps.
}
\keyword{internal}
