% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_indices.R
\name{refresh_climate_index}
\alias{refresh_climate_index}
\title{Re-download cached climate indices}
\usage{
refresh_climate_index(index = NULL)
}
\arguments{
\item{index}{one or more index names, or \code{NULL} for every living index}
}
\value{
invisibly, a data frame of what was refreshed and where it now ends
}
\description{
Forces a fresh copy, ignoring the cache. Use after a provider publishes, or
when a series looks like it has stopped short.
}
\details{
Indices that will never change are skipped rather than re-fetched. \code{LCR} was
published with a paper and ends at 2014, so downloading it again cannot
produce anything new.
}
\examples{
\dontrun{
refresh_climate_index()          # every index that is still growing
refresh_climate_index("AMOC")
}
}
\seealso{
\code{\link[=climate_index_status]{climate_index_status()}}, \code{\link[=fetch_climate_index]{fetch_climate_index()}}
}
