% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bathymetry.R
\name{marmap_cache_file}
\alias{marmap_cache_file}
\title{Where marmap would have cached this grid}
\usage{
marmap_cache_file(bounding_box, resolution, path)
}
\arguments{
\item{bounding_box}{named list with \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}}

\item{resolution}{grid resolution in arc-minutes}

\item{path}{the cache directory}
}
\value{
the path marmap would use, or \code{NULL} where the convention does not
apply
}
\description{
Rebuilds the filename \code{marmap::getNOAA.bathy()} writes, so an existing
download can be read directly instead of requested again.
}
\details{
The convention is marmap's, not this package's: \code{marmap_coord_} then the
bounding box as \verb{x1;y1;x2;y2} with the lower corner first, then \verb{_res_} and
the resolution. Requests that cross the antimeridian get an \verb{_anti} suffix,
and are not claimed here — returning \code{NULL} for them means falling back to
\code{getNOAA.bathy()}, which is the correct answer either way.

Should marmap ever change the convention, this stops matching and every fetch
goes back through \code{getNOAA.bathy()}. That is slower and chattier, not wrong.
}
\keyword{internal}
