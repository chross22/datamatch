% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obdaac.R
\name{obdaac_assemble}
\alias{obdaac_assemble}
\title{Turn cached OB.DAAC subsets into the object a fetch returns}
\usage{
obdaac_assemble(paths, sensor, resolution, frequency)
}
\arguments{
\item{paths}{\if{html}{\out{<char>}} cached subset files}

\item{sensor, resolution, frequency}{\if{html}{\out{<char>}} what produced them}
}
\value{
\if{html}{\out{<sf object>}} one row per grid cell per time step, stamped with its
step and its source
}
\description{
Separate from \code{\link[=accessOBDAAC]{accessOBDAAC()}} because a call that finds everything already
cached returns here without touching the network, credentials or the file
search - and that path has to build exactly the same object as the one that
downloaded it.
}
\keyword{internal}
