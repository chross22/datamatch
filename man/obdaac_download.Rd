% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obdaac.R
\name{obdaac_download}
\alias{obdaac_download}
\title{Download one OB.DAAC file, refusing the login page}
\usage{
obdaac_download(file, destination, credentials)
}
\arguments{
\item{file}{\if{html}{\out{<char>}} the filename to fetch}

\item{destination}{\if{html}{\out{<char>}} where to write it}

\item{credentials}{from \code{\link[=obdaac_credentials]{obdaac_credentials()}}}
}
\value{
\code{NULL} on success, or a one-line description of the failure
}
\description{
Download one OB.DAAC file, refusing the login page
}
\section{Why the status code is not enough}{

An unauthenticated request is answered with \strong{HTTP 200 and the Earthdata
Login page}, not with a 401. A download that trusts the status code
therefore writes nine kilobytes of HTML into a file named \code{.nc}, and the
mistake surfaces much later as a corrupt netCDF, naming neither the
credential nor the file.

The first bytes are checked instead. Every file here is netCDF, which begins
either \code{CDF} or the HDF5 signature, and an HTML document begins with
neither.
}

\keyword{internal}
