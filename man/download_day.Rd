% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copernicus.R
\name{download_day}
\alias{download_day}
\title{Download one day's subset, returning any failure rather than raising it}
\usage{
download_day(item, dataset_id, vars, bounding_box, depth, hourly = FALSE)
}
\arguments{
\item{item}{\if{html}{\out{<list>}} one work item: \code{ofile}, \code{time}, and the calendar fields}

\item{dataset_id}{\if{html}{\out{<char>}} Copernicus dataset identifier}

\item{vars}{\if{html}{\out{<char>}} variable codes to request}

\item{bounding_box}{passed to \code{bb}}

\item{depth}{\if{html}{\out{<numeric>}} length-2 depth range in metres}

\item{hourly}{\if{html}{\out{<logical>}} whether the dataset is hourly, in which case the
request covers the whole day rather than one instant. A bare date means
midnight to midnight, which on an hourly dataset selects the first hour and
silently discards the other 23.}
}
\value{
\code{NULL} on success, or a one-line description of the failure
}
\description{
Defined at the top level rather than as a closure inside \code{\link[=accessCopernicus]{accessCopernicus()}} so
that its enclosing environment is this package's namespace. A closure would
carry \code{accessCopernicus()}'s whole evaluation frame — including the cluster
object itself — to every worker when it is serialised.
}
\details{
Errors are caught and returned as text. Under \code{\link[parallel:parLapplyLB]{parallel::parLapplyLB()}} an
error thrown in a worker aborts the entire batch, so a single bad day would
discard the days still in flight. Collecting failures instead lets every day
be attempted, and lets the caller be told about all of them at once rather
than about whichever one happened to fail first.
}
\keyword{internal}
