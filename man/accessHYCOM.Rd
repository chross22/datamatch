% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hycom.R
\name{accessHYCOM}
\alias{accessHYCOM}
\title{Access HYCOM output from the GOFS 3.1 reanalysis}
\usage{
accessHYCOM(
  vars,
  years = NULL,
  months = NULL,
  bounding_box,
  dates = NULL,
  frequency = c("daily", "3hourly"),
  hour = 12L,
  archive = "GLBv53X",
  overwrite = FALSE
)
}
\arguments{
\item{vars}{\if{html}{\out{<char>}} variables to read, from \code{\link[=hycom_variables]{hycom_variables()}}}

\item{years}{\if{html}{\out{<numeric>}} years to read. Required unless \code{dates} is given.}

\item{months}{\if{html}{\out{<numeric>}} months to read. Required unless \code{dates} is given.}

\item{bounding_box}{\if{html}{\out{<list>}} named list with \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}, or
an \code{sf}/\code{sfc} object to take the bounding box of. Longitudes are negative
west, as elsewhere in this package.}

\item{dates}{the exact dates to read, as \code{YYYYMMDD} strings, \code{YYYY-MM-DD}
strings, or \code{Date} objects}

\item{frequency}{\if{html}{\out{<char>}} \code{"daily"} (the default) for one snapshot per day, or
\code{"3hourly"} for every step. Neither is a mean; see the Three-hourly section.}

\item{hour}{\if{html}{\out{<integer>}} which UTC hour to take when \code{frequency = "daily"}. Must
be a multiple of 3, since that is the model's step.}

\item{archive}{\if{html}{\out{<char>}} which archive to read, from \code{\link[=hycom_archives]{hycom_archives()}}. The
default is the 1994–2015 reanalysis; later years live in the operational
archives, which \code{\link[=hycom_covering]{hycom_covering()}} will name for a given date.}

\item{overwrite}{\if{html}{\out{<logical>}} re-read time steps already cached}
}
\value{
\if{html}{\out{<sf object>}} one row per grid cell per time step, with \code{YEAR}, \code{MONTH}
and \code{DAY}, an \code{HOUR} column when \code{frequency = "3hourly"}, and a column per
requested variable
}
\description{
Reads HYCOM over OPeNDAP and returns it as an \code{sf} point object with one row
per grid cell and time step — the same shape \code{\link[=accessCopernicus]{accessCopernicus()}}, \code{\link[=accessCCMP]{accessCCMP()}},
\code{\link[=accessERDDAP]{accessERDDAP()}} and
\code{\link[=accessFVCOM]{accessFVCOM()}} return, so \code{\link[=matchData]{matchData()}} joins it unchanged.
}
\section{Why reach for it}{

Two reasons, both about what the Copernicus reanalysis lacks. HYCOM publishes
\strong{bottom salinity and bottom temperature as fields}, where GLORYS12V1 has
only bottom temperature and \code{BOTS} has to be derived from the full depth
column. And it is an \strong{independent model}, so agreement between it and
Copernicus is evidence about a result in a way that either one alone is not.

A covariate taken from HYCOM is \strong{not interchangeable} with the same-named
covariate from Copernicus or FVCOM, though this returns it in a column of the
same name. Three different models. Say which you used.
}

\section{Three-hourly, and no monthly mean}{

HYCOM publishes instantaneous fields every three hours. There is no monthly
or daily mean to fetch, so this does not offer one:

\itemize{
\item \code{frequency = "daily"} (the default) takes \strong{one snapshot per day}, at
the hour given by \code{hour}. It is an instant, not a daily mean, and a
12:00 UTC snapshot of a tidal shelf sea is not the day's average.
\item \code{frequency = "3hourly"} returns every step, with an \code{HOUR} column.
}

A real mean is then \code{\link[=upscale_time]{upscale_time()}}'s job, which keeps the aggregation
visible and the choice of summary yours:

\if{html}{\out{<div class="sourceCode">}}\preformatted{steps <- accessHYCOM(vars = "SST", frequency = "3hourly", years = 2010,
                     months = 6, bounding_box = bb)
daily <- upscale_time(steps, to = "day")     # a genuine daily mean
}\if{html}{\out{</div>}}

Fetching a month of three-hourly data is 248 downloads over a slow protocol.
Prefer \code{dates} and a small box unless the whole series is genuinely wanted.
}

\section{Reaching past 2015}{

The default archive is the \strong{reanalysis}, \code{GLBv53X}, which is one
internally consistent run over 1994–2015. HYCOM continues to the present, but
as a chain of shorter \strong{operational} experiments — the model as it was
running at the time. \code{\link[=hycom_archives]{hycom_archives()}} lists them and \code{\link[=hycom_covering]{hycom_covering()}}
says which span a given date:

\if{html}{\out{<div class="sourceCode">}}\preformatted{hycom_covering("2019-06-15")
#> [1] "GLBv930" "GLBy930"

recent <- accessHYCOM(vars = "BOTS", dates = "2019-06-15",
                      bounding_box = bb, archive = "GLBy930")
}\if{html}{\out{</div>}}

One archive is read per call, and a request falling outside the one named is
told which others hold it rather than being stitched to them silently. The
archives overlap, so where two cover a date there is a real choice between
the more consistent run and the more recent one, and crossing from the
reanalysis into an operational run is a discontinuity in how the values were
made. The grids themselves agree through the middle latitudes, so on a shelf
the cells line up across the seam even though the runs do not — see
\code{\link[=hycom_archives]{hycom_archives()}}.
}

\section{One dataset per year, sometimes}{

The reanalysis is published one dataset per year, so a request spanning years
opens one connection per year. The operational archives are each a single
aggregation and open once.
}

\examples{
\dontrun{
bb <- list(xmin = -70, xmax = -66, ymin = 41, ymax = 44)

# Bottom salinity, which the Copernicus reanalysis cannot serve directly
bottom <- accessHYCOM(vars = c("BOTT", "BOTS"), years = 2010, months = 1:12,
                      bounding_box = bb)

matched <- matchData(observations, bottom)

# Every three-hourly step, then a genuine daily mean
steps <- accessHYCOM(vars = "SST", frequency = "3hourly",
                     dates = "2010-06-15", bounding_box = bb)
daily <- upscale_time(steps, to = "day")
}
}
\seealso{
\code{\link[=hycom_variables]{hycom_variables()}} for what can be read, \code{\link[=accessCopernicus]{accessCopernicus()}},
\code{\link[=accessFVCOM]{accessFVCOM()}}, \code{\link[=accessCCMP]{accessCCMP()}} and \code{\link[=accessERDDAP]{accessERDDAP()}} for the other sources,
\code{\link[=matchData]{matchData()}} for joining any of them
}
