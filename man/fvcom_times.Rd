% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fvcom.R
\name{fvcom_times}
\alias{fvcom_times}
\title{Time steps of an FVCOM archive, as dates}
\usage{
fvcom_times(handle)
}
\arguments{
\item{handle}{an open \code{ncdf4} handle}
}
\value{
a \code{Date} vector, one per time step
}
\description{
Computed from \code{Itime}, the integer day count, and not from the \code{Times}
character axis beside it. Both are present and \code{Times} is the more obvious
choice, but on the 30-year GOM3 mean it is corrupt: the first step reads
\verb{1878-01-14T03:009//-500000}, a century out with a mangled time. Parsing it
would silently place the whole record in the wrong era.
}
\details{
\code{Itime} is sound, and its \code{units} attribute declares the epoch
(\verb{days since 1858-11-17}, the Modified Julian Day epoch). The epoch is read
from that attribute rather than assumed, so an archive counting from some
other origin is handled rather than misread.
}
\keyword{internal}
