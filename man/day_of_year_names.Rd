% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables.R
\name{day_of_year_names}
\alias{day_of_year_names}
\title{Column names that mean day-of-year, not year or day-of-month}
\usage{
day_of_year_names
}
\value{
\if{html}{\out{<char>}} lower-case day-of-year column names
}
\description{
\code{standardize_time_columns()} finds a table's time columns by prefix, which
sweeps in names that look right and mean something else: \code{yearday} begins with
\code{year}, \code{dayofyear} with \code{day}. Both hold an ordinal day, so accepting either
would put every row in a period no \code{source} covers - an all-NA join behind a
vague warning rather than an error.
}
\details{
Names here are never candidates for any time column. They are still reported
as near misses when nothing else matches, so a caller who did mean one of them
is told to rename it rather than left guessing.

Compared lower-case, so casing in the data does not matter. Extend it when
another convention turns up; the only effect of a name being listed is that
\code{matchData()} declines to guess.
}
\keyword{internal}
