% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hycom.R
\name{hycom_window}
\alias{hycom_window}
\title{The contiguous index window covering a bounding box}
\usage{
hycom_window(values, lower, upper, axis)
}
\arguments{
\item{values}{\if{html}{\out{<numeric>}} the axis coordinates, increasing}

\item{lower, upper}{the requested range}

\item{axis}{\if{html}{\out{<char>}} the axis name, for the error}
}
\value{
\code{list(start =, count =, values =)}, one-based
}
\description{
HYCOM is a regular grid whose coordinates increase monotonically, so a box is
a contiguous run of indices on each axis and can be asked for as a slice.
That is the opposite of FVCOM, whose mesh numbering is not spatially coherent
and has to be read whole — see \code{\link[=fvcom_in_box]{fvcom_in_box()}}.
}
\details{
Computed from the coordinate values rather than from a step, because HYCOM's
latitude spacing is not uniform: it is finer near the equator than toward the
poles, so anything derived from \code{lat[2] - lat[1]} would be wrong at most
latitudes.
}
\keyword{internal}
