% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copernicus_cli.R
\name{download_copernicus_subset}
\alias{download_copernicus_subset}
\title{Download a Copernicus subset through the command line client}
\usage{
download_copernicus_subset(
  dataset_id,
  vars,
  bb,
  depth,
  time,
  ofile,
  overwrite = TRUE,
  tries = 3,
  wait = 10,
  verbose = FALSE,
  dry_run = FALSE,
  log_level = "ERROR"
)
}
\arguments{
\item{dataset_id}{\if{html}{\out{<char>}} Copernicus dataset identifier}

\item{vars}{\if{html}{\out{<char>}} variable codes to request}

\item{bb}{bounding box: a named list or vector with \code{xmin}, \code{xmax}, \code{ymin},
\code{ymax}, or an \code{sf}/\code{sfc} object to take the bounding box of}

\item{depth}{\if{html}{\out{<numeric>}} length-2 depth range in metres}

\item{time}{\if{html}{\out{<Date>}} or \if{html}{\out{<char>}} length 1 or 2; a single value requests one instant}

\item{ofile}{\if{html}{\out{<char>}} full path of the file to write}

\item{overwrite}{\if{html}{\out{<logical>}} pass \code{--overwrite}}

\item{tries}{\if{html}{\out{<integer>}} number of attempts before giving up}

\item{wait}{\if{html}{\out{<numeric>}} seconds to wait between attempts}

\item{verbose}{\if{html}{\out{<logical>}} echo the command and the client's output}

\item{dry_run}{\if{html}{\out{<logical>}} pass \code{--dry-run}, which validates the request against
the API without downloading anything}

\item{log_level}{\if{html}{\out{<char>}} one of the client's levels: DEBUG, INFO, WARN, ERROR,
CRITICAL, QUIET. \code{ERROR} is the default rather than \code{QUIET}, because the
client says \emph{why} a request failed at ERROR and says nothing at all at
QUIET. A date past the end of a reanalysis, for instance, exits non-zero
with no output under QUIET, leaving the caller an empty failure to explain.}
}
\value{
\code{0}, invisibly, on success; errors otherwise
}
\description{
Wraps one \verb{copernicusmarine subset} call. Arguments are passed to \code{\link[=system2]{system2()}}
as a character vector rather than pasted into a shell string, so paths
containing spaces are handled by the process boundary instead of by quoting.
}
\section{Retries}{

The API is intermittently unavailable under load, and a failed request is
usually transient. Failures are retried \code{tries} times with a pause between,
and only a run of failures is raised as an error.

That error carries the client's own output. A download that fails and returns
quietly would surface later as an unreadable file, at the point of
\code{terra::rast()}, with nothing left to say why.
}

\keyword{internal}
