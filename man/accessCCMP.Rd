% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccmp.R
\name{accessCCMP}
\alias{accessCCMP}
\title{Access CCMP ocean surface winds}
\usage{
accessCCMP(
  vars,
  years = NULL,
  months = NULL,
  bounding_box,
  dates = NULL,
  frequency = c("daily", "6hourly"),
  hour = 12L,
  version = "v03.1",
  overwrite = FALSE
)
}
\arguments{
\item{vars}{\if{html}{\out{<char>}} variables to read, from \code{\link[=ccmp_variables]{ccmp_variables()}}}

\item{years}{\if{html}{\out{<numeric>}} years to read. Required unless \code{dates} is given.}

\item{months}{\if{html}{\out{<numeric>}} months to read. Required unless \code{dates} is given.}

\item{bounding_box}{\if{html}{\out{<list>}} named list with \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}, or
an \code{sf}/\code{sfc} object. Longitudes negative west.}

\item{dates}{the exact dates to read, as \code{YYYYMMDD} strings, \code{YYYY-MM-DD}
strings, or \code{Date} objects}

\item{frequency}{\if{html}{\out{<char>}} \code{"daily"} (the default) for one snapshot per day, or
\code{"6hourly"} for all four steps. Neither is a mean.}

\item{hour}{\if{html}{\out{<integer>}} which UTC hour to take when \code{frequency = "daily"}.
Must be 0, 6, 12 or 18.}

\item{version}{\if{html}{\out{<char>}} which CCMP version to read, from \code{\link[=ccmp_versions]{ccmp_versions()}}}

\item{overwrite}{\if{html}{\out{<logical>}} re-read days already cached}
}
\value{
\if{html}{\out{<sf object>}} one row per grid cell per time step, with \code{YEAR},
\code{MONTH}, \code{DAY}, an \code{HOUR} column when \code{frequency = "6hourly"}, and a column
per requested variable
}
\description{
Downloads the CCMP wind analysis from Remote Sensing Systems and returns it
as an \code{sf} point object with one row per grid cell and time step — the same
shape \code{\link[=accessCopernicus]{accessCopernicus()}}, \code{\link[=accessFVCOM]{accessFVCOM()}}, \code{\link[=accessHYCOM]{accessHYCOM()}} and \code{\link[=accessERDDAP]{accessERDDAP()}}
return, so
\code{\link[=matchData]{matchData()}} joins it unchanged.
}
\section{Why reach for it, and why not}{

CCMP is the longest consistent surface wind record available here. It runs
from \strong{January 1993 to within days of the present}, six-hourly throughout,
which is both longer and finer in time than the Copernicus L4 wind — that is
monthly from mid-1994, or hourly only from 2007, with nothing daily in
between.

Against that: \strong{CCMP carries no wind stress.} The Copernicus product does,
and stress rather than speed is what drives mixing and Ekman pumping. Stress
cannot be recovered from these winds without choosing a drag coefficient,
which is a modelling decision rather than a unit conversion. Where stress is
the covariate you want, use the Copernicus wind.

The two are different analyses of the same quantity, so a wind from CCMP is
\strong{not interchangeable} with one from Copernicus even though both arrive in a
\code{UWND} column. Say which you used.
}

\section{It downloads the whole globe}{

RSS publishes CCMP as static files with no OPeNDAP endpoint, so there is no
way to ask for a region. \strong{A day is one 33 MB global file} however small the
bounding box, and the subset is taken locally. A year is therefore about
12 GB of transfer to keep a few megabytes of it.

The extracted subset is cached, so this is paid once per day of data. A
request for more than 30 days says what it is about to download before
starting.
}

\section{Six-hourly, and no mean to fetch}{

CCMP is an analysis at 00, 06, 12 and 18 UTC. There is no daily or monthly
mean in the archive, so none is invented:

\itemize{
\item \code{frequency = "daily"} (the default) takes one \strong{snapshot}, at \code{hour}.
\item \code{frequency = "6hourly"} returns all four steps, with an \code{HOUR} column.
}

A real mean is \code{\link[=upscale_time]{upscale_time()}}'s job, which keeps it visible:

\if{html}{\out{<div class="sourceCode">}}\preformatted{steps <- accessCCMP(vars = c("UWND", "VWND"), frequency = "6hourly",
                    dates = "2010-06-15", bounding_box = bb)
daily <- upscale_time(steps, to = "day")
}\if{html}{\out{</div>}}

Note that a mean of \code{UWND} and \code{VWND} is not a mean \code{WSPD}. Averaging the
components and taking the magnitude gives the net displacement of air;
averaging the speed gives how hard it blew. On a day the wind reversed, the
first is near zero and the second is not.
}

\section{Longitude convention}{

CCMP is stored on a 0-360 grid, alone among the sources here. \code{bounding_box}
is given negative west as everywhere else in this package, converted on the
way in, and the returned coordinates are negative west too — so the result
overlays the other sources without adjustment.
}

\examples{
\dontrun{
bb <- list(xmin = -70, xmax = -66, ymin = 41, ymax = 44)

wind <- accessCCMP(vars = c("UWND", "VWND", "WSPD"),
                   dates = unique(observations$date), bounding_box = bb)

matched <- matchData(observations, wind)
}
}
\seealso{
\code{\link[=ccmp_variables]{ccmp_variables()}}, \code{\link[=accessCopernicus]{accessCopernicus()}} for the Copernicus wind, which
carries stress
}
