% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessEnvDat.R
\name{without_calendar_warning}
\alias{without_calendar_warning}
\title{Evaluate something, muffling only terra's unknown-calendar warning}
\usage{
without_calendar_warning(expr)
}
\arguments{
\item{expr}{an expression to evaluate}
}
\value{
the value of \code{expr}
}
\description{
Some Copernicus files declare their calendar as \code{Gregorian}, which is not one
of the spellings the CF conventions list. terra warns and assumes the standard
calendar, which is correct — \code{Gregorian} \emph{is} the standard calendar, spelled
with a capital — so the warning says nothing a caller can act on. One per file
makes fifty on a fifty-day fetch, burying the warnings that do matter.
}
\details{
Matched on the message text so that only this one is caught. Every other
warning is left to propagate, since a file that is genuinely unreadable should
still say so.
}
\keyword{internal}
